# API
API_KEY=
# Leer = kein Schutz. Gesetzt = API-Key erforderlich für geschützte Endpunkte.

# Datenbank
DATABASE_URL=postgresql+asyncpg://user:password@db:5432/purzel

# Bildgenerierung
PICSART_API_KEY=
HF_TOKEN=
REPLICATE_API_TOKEN=

# Mesh-Generierung (HF_TOKEN wird wiederverwendet für HF Spaces)
# PiAPI (optional) — TRELLIS Mesh-Provider via REST-API (Pay-as-you-go)
# Ohne Token wird trellis-piapi nicht in der Provider-Liste angezeigt.
PIAPI_API_KEY=

# Rigging
# UniRig Lokal (optional, für GPU-Inferencing ohne HF Space)
# Checkpoint herunterladen (einmalig, ~5 GB):
#   huggingface-cli download VAST-AI/UniRig --local-dir ./models/unirig/
# UniRig-Repo klonen für Inferenz-Skripte:
#   git clone https://github.com/VAST-AI-Research/UniRig ./unirig/
UNIRIG_MODEL_PATH=./models/unirig/
UNIRIG_REPO_PATH=./unirig/

# AI-Agenten (Prompt-Assistent, Auto-Tagging, Qualitätsbewertung, Workflow-Empfehlung)
# Ohne Token geben alle /agents/*-Endpunkte 503 zurück. Server startet trotzdem.
ANTHROPIC_API_KEY=

# Integrationen
SKETCHFAB_API_TOKEN=
# Sketchfab: API-Token für Upload/Import von 3D-Modellen. Ohne Token werden Features ausgeblendet.

# Blender Rigify (lokal, headless) — für blender-rigify Provider
# Standard: blender (aus PATH). Überschreibbar für custom Pfade.
BLENDER_EXECUTABLE=blender

# Server
ALLOWED_ORIGINS=http://localhost:5173
# CORS: kommagetrennte Liste erlaubter Ursprünge
# Beispiel: http://localhost:5173,https://meine-app.example.com
CORS_ORIGINS=http://localhost:5173

STORAGE_PATH=./storage
# Basis-Pfad für Meshes, BgRemoval, Assets, Animations, rembg_cache
